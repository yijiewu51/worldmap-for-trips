# 📤 分享给女朋友的完整指南

## 🎯 方法一：直接分享文件夹（最简单）

### 步骤1：打包文件夹

1. 找到 `worldmap for trips` 文件夹
2. 右键点击文件夹
3. 选择"压缩"（Mac）或"发送到 → 压缩文件夹"（Windows）
4. 会生成一个 `.zip` 文件

### 步骤2：发送给她

- 通过微信发送压缩包
- 通过邮件发送
- 通过其他方式（QQ、网盘等）

### 步骤3：她如何使用

1. 解压文件夹
2. 双击打开 `index.html` 文件
3. 输入密码：`20251213`
4. 点击"进入"
5. 开始使用！

**完成！** 现在她就可以使用了。

---

## 🎯 方法二：使用导出/导入功能（推荐，可以同步数据）

### 第一次分享

1. **你操作**：
   - 打包整个文件夹发送给她（同方法一）
   - 让她先设置好用户信息（名称和头像）

2. **之后同步数据**：
   - 你添加完地点后，点击右上角"📤 导出"按钮
   - 会下载一个 JSON 文件
   - 发送给她

3. **她操作**：
   - 收到 JSON 文件后
   - 点击右上角"📥 导入"按钮
   - 选择你发送的 JSON 文件
   - 自动合并数据

### 双向同步

- 她也可以导出数据发送给你
- 你导入后就能看到她的地点
- 建议每周或每添加几个地点后同步一次

---

## 📝 使用说明（告诉她）

### 基本操作

1. **添加地点**：
   - 点击"➕ 添加地点"按钮
   - 选择是"用户1"还是"用户2"添加的
   - 输入地点名称，点击"搜索坐标"自动获取位置
   - 填写其他信息后保存

2. **查看地点**：
   - 点击地图上的标记
   - 或点击侧边栏的地点卡片

3. **编辑地点**：
   - 点击地点查看详情
   - 点击"编辑"按钮
   - 修改后保存

4. **删除地点**：
   - 点击地点查看详情
   - 点击"删除"按钮
   - 确认删除

5. **筛选地点**：
   - 使用顶部的筛选按钮
   - "全部"：显示所有地点
   - "已去过"：只显示已去过的地方
   - "想去"：只显示想去的地方
   - "按用户"：只显示当前用户添加的地点

6. **切换用户**：
   - 点击右上角的"切换"按钮
   - 可以在两个用户之间切换

7. **设置用户信息**：
   - 点击右上角"⚙️ 设置"按钮
   - 设置用户名称和上传头像

---

## 🔑 重要信息

- **密码**：`20251213`（默认密码）
- **数据存储**：数据保存在各自的浏览器中
- **数据同步**：通过导出/导入功能同步
- **备份**：建议定期导出数据作为备份

---

## 🆘 常见问题

**Q: 她忘记密码怎么办？**
A: 点击"重置密码"按钮，会清除所有数据并使用默认密码。

**Q: 数据会丢失吗？**
A: 数据保存在浏览器本地，清除浏览器数据会丢失。建议定期导出备份。

**Q: 可以同时使用吗？**
A: 可以，但数据不会自动同步，需要通过导出/导入功能手动同步。

---

## 💡 使用建议

1. **首次使用**：让她先设置好自己的用户名称和头像
2. **定期同步**：建议每周或每添加几个地点后同步一次
3. **数据备份**：定期导出数据作为备份
4. **互相分享**：她也可以导出数据发送给你，实现双向同步

---

祝你们旅行愉快！🎉

