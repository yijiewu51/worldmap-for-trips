# 📝 GitHub Gist 创建详细步骤（图文说明）

## 🎯 第一步：访问 GitHub Gist

1. 打开浏览器，访问：https://gist.github.com/
2. 如果还没登录，点击右上角 "Sign in" 登录
3. 如果没有账号，点击 "Sign up" 注册（不需要邮箱验证码）

## 🎯 第二步：创建新的 Gist

登录后，你会看到这个页面：

```
┌─────────────────────────────────────────┐
│  [文件名输入框]                          │
│  gistfile1.txt                         │
├─────────────────────────────────────────┤
│                                         │
│  [内容输入框 - 大文本框]                 │
│                                         │
│                                         │
└─────────────────────────────────────────┘
```

### 具体操作：

1. **在文件名输入框中**（最上面的小框）：
   - 删除默认的 `gistfile1.txt`
   - 输入：`travel-map-data.json`

2. **在内容输入框中**（下面的大框）：
   - 输入：`{}`
   - 就这两个字符：左大括号 `{` 和右大括号 `}`

3. **选择公开/私密**：
   - 在页面底部，选择 **"Create public gist"**（公开）
   - 或者选择 "Create secret gist"（私密）也可以

4. **点击创建**：
   - 点击绿色的 **"Create public gist"** 按钮

## 🎯 第三步：获取 Raw URL

创建成功后，页面会跳转，你会看到：

```
┌─────────────────────────────────────────┐
│  travel-map-data.json                   │
│  [Raw] [Download] [Copy] [Star]        │
├─────────────────────────────────────────┤
│  {}                                     │
└─────────────────────────────────────────┘
```

### 获取URL的方法（两种都可以）：

**方法1：点击 Raw 按钮**
1. 点击文件内容右上角的 **"Raw"** 按钮
2. 页面会跳转到一个只显示 `{}` 的页面
3. 复制浏览器地址栏的URL（类似：`https://gist.githubusercontent.com/username/abc123.../raw/...`）

**方法2：直接复制地址栏**
1. 在Gist页面，点击文件名 `travel-map-data.json`（蓝色链接）
2. 复制浏览器地址栏的URL
3. 如果URL中没有 `/raw/`，手动在URL后面加上 `/raw/`

## 🎯 第四步：配置应用

1. 打开你的旅行地图应用页面
2. 按 `F12`（Windows）或 `Cmd+Option+J`（Mac）打开浏览器控制台
3. 在控制台输入（记得替换成你复制的URL）：

```javascript
localStorage.setItem('travelMapGistUrl', 'https://gist.githubusercontent.com/你的用户名/你的gist-id/raw/你的文件hash/travel-map-data.json')
```

4. 按回车
5. 刷新页面

## ✅ 完成！

现在你的应用已经配置好了！右上角应该显示"已同步（云端）"。

---

## 🆘 常见问题

**Q: 找不到文件名输入框？**
A: 在页面最上方，有一个小的输入框，默认显示 `gistfile1.txt`

**Q: 创建后找不到 Raw 按钮？**
A: 在文件内容区域的右上角，有几个小按钮，其中一个就是 "Raw"

**Q: URL 格式不对？**
A: 确保URL包含 `/raw/`，格式应该是：`https://gist.githubusercontent.com/用户名/ID/raw/文件hash/文件名.json`

**Q: 还是不会？**
A: 告诉我你卡在哪一步，我可以详细指导！

